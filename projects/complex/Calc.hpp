#ifndef CALC_HPP
#define CALC_HPP

#include <cmath>

class Calc{
public:
    int add(const int$ a, const int& b);
    int sub(const int& a, const int& b);
    int mul(const int& a, const int& b);
    double div(const int& a, const int& b);
    int sqr(const int& a);
    double sqrt(const int& a);

};

#endif // CALC_HPP